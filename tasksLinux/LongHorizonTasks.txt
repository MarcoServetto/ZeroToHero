Task:
Do not execute the following task, but reason about it to find mistakes and places where the instructions are contraddictory or could be more clear:
----------
Add
Foo:Main,OpenWith[AutoloadedIcon,"extension"]{}
Foo:Main,Shortcut[AutoloadedIcon,"extension"]{}
Foo:Main,OpenWith[AutoloadedIcon]{}
Foo:Main,Shortcut[AutoloadedIcon]{}
If no extension, the manager auto assign one from a list.
(Ignore for now:On MacOs (eventually) we may have to ignore the user provided extensions that are not in the default list, and ignore the icon if not in the default list.)

base has
OpenWith[Icon,Extension]{}
Shortcut[Icon,Extension]{}
OpenWith[Icon]{}
Shortcut[Icon]{}
Type system checks they are only implemented on something that extends (even indirectly) Main; see how it works for Sealed etc with how to add it to the TypeSystem
Type system will check that the Extension is a text that respects the intended format for extensions (see what is allowed to be in a project)
Type system will check that the Icon is indeed an autoloaded Png, square and with an appropriate size (help me decide the appropriate range)

When fearless portable compiles something with OpenWith/Shortcut: no change, no behaviour is induced by the portable for those interfaces.

The manager when compiling something with OpenWith will:
- Only on windows: use the existing Java code to create a .ico out of a .png and save that .ico in an appropriate place, somewhere in .fearless_out.

- add an association to that Icon with the fearless manager.
- in the info file it will remember the association (extend the format as appropriate)
- in the gui, it will be displayed that the association exists.
- editing the file removing the association will signal that the modification need to happen in the code with an error and ignore the mutation as if the new Info file was invalid.
(Ignore for now: I wonder if this new mind set allows us to move more/all the information in the info file)

-when the fearless manager is called with such an association it will:
 1: Invoke the main of that application normally.
 (Ignore for now: 2: use the existing capability API to feed files into a running app to feed this file to this running main)

-Compiling with Shortcut is actually exactly the same; but in addition a file
 "foo.extension" (where foo is the name of the main implementing Shortcut twisted to be valid in the projects exactly the inverse way auto imported assets are twisted to be valid type names, and extension is the user provided second parameter to Shortcut, or the autoselected one by the manager)
 will be created (empty) in the top level of the fearless project containing this main.
 The manager gui will then show the icons of the 'shortcutted mains' in the view for that project and double clicking on them will start them. 


Autoselecting: the manager will select for Shortcut an extension of the form fappNNN
where NNN are numbers 0-9 and the smallest number that is a non fearless assigned extension is selected.
For files it will select ffileNNN instead.

Add unit tests when reasonable.
Open the needed PRs to import your results.
---------