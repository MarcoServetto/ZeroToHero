Task:
Consider the minigame in ZeroToHero.

Walking; all the walking levels:
Some of the walking level use the concept that 'something is ok' even if there is something broken around if there is an good enough 'ok context around this'.
This is a non obvious extra difficulty and I wonder how easy it would be to simplify the puzzles and never rely on this strange corner case, so that if anything wrong is in the text, the correct answer is just 'error'.
Can you try to understand in all the walking levels how many times we use this corner case and why?

Note how we do rely on 'the whole text area can be interpreted as both method body or top level' and we do not really tell the user what of the two.
For example
`East`
Is an object literal if we are in a method body, but an error if we are at top level. Not sure hot to logically sort this issue. It does feel natural to offer both
1 + 2
and
North:Direction{}
as puzzles, but technically it is wrong... not sure how to navigate this.
Can you search what other peoples are doing in similar set ups?
Can you provide a good opinion?
Overall save your results as a github page in marcoautomation2

Task:
Consider the minigame in ZeroToHero.

Walking and level 102
Making the user understanding walking is challenging.
The first question was design for it, but it fails and users are still confused.
New first question:

// You need to do two actions:
//(1) Select the correct text
//(2) press the correct button

//Select all and only this line

//Press the button 'comment'

Where the special char can be the last 'e' of the line.

If the user fails the very first for more then 3 times, we should get the same 'example' button we have now at the beginning of level 101, in the same position with a similar 'show' of the intended action.

Test this using the actual desktop (no other agent is using it right now)
and real simulated mouse and key events.

Open a Pr with your suggested code changes.

Task:
Consider the minigame in ZeroToHero and the currently opened PR.
Test this using the actual desktop (no other agent is using it right now)
and real simulated mouse and key events.

Walking

if the user clicks a button with selection of size one (that is, they most likely did not attempt any selection, just the 'special char' is selected by default),
- if the solution is indeed to keep the selection of size one, no change with respect to current behaviour. Else
1- the click does not count as an attempt (so it is not 'wrong' and no streak is lost)
2- panic should appear (see DirectInstructions and get the same effect) and say `select text first\nthen press button`.

Amend the open a Pr with your suggested code changes.


Task:
Consider the minigame in ZeroToHero and the currently opened PR.
Test this using the actual desktop (no other agent is using it right now)
and real simulated mouse and key events.

Walking
When there is a question whose answer is 'error', and the user press any other button (so it fails) 
In addition to the current failing animation, while the solution is shown, Panic should show up
(see DirectInstructions and get the same effect)
and report a message explaining why it was an error. This means that the Java EDSL need to be changed to require an extra string when a question answer is 'error'.


Amend the open a Pr with your suggested code changes.

Task:
Consider the minigame in ZeroToHero and the currently opened PR.
Test this using the actual desktop (no other agent is using it right now)
and real simulated mouse and key events.

Walking
A typical mistake players do is that they forget they have to select the smallest self contained syntactical unit.
When 'wrong because too much is select'  1/3 of the times panic come up with 'remember, the smallest syntactically...'

Minor tweak: can you add just a little bit of left padding so that the text in the main walking text area is not fully attached to the side?

Amend the open a Pr with your suggested code changes.



Task:
As a user, using the browser, explore
https://marcoservetto.github.io/ZeroToHero/src/assetsGuide/01_01.html

and check if the layout is ever 'broken'.
If you find any issue, open a PR to fix this.