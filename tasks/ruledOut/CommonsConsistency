The three null checks have different roles and all stay.

Good:
  return Objects.requireNonNull(image);
  assert Require.nonNull(a, b, c);
  assert x != null;

Bad:
  assert image != null;
  return image;

Bad:
  assert a != null && b != null && c != null;
