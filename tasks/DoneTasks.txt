Completed long-horizon tasks land here, moved out of LongHorizonTasks.txt by
the overnight loop once a subagent finishes them (whether the outcome was
success or failure -- a failed task is still "done"; its report explains
what went wrong so Marco can reword it and put it back in
LongHorizonTasks.txt if he wants another attempt).

Each entry: the original task text, followed by its report (what was done,
where results/PRs/issues/pages ended up, or the failure explanation).

---

Task:
Look at the code generating the Html docs when a fearless package is
compiled. Add a companion generation that generate minimal formatted
text; designed to reduce text and minimise it to save tokens and to
make this text a great input for a coding agent needing that docs to
write code in Fearless. Basically plain text instead of HTML.
Make sure to write in CLAUDE.md how to locate those files, so that it is easier to code in Fearless by looking to those instead of the full implementation of the standard library
Make a PR on the original repo so that those files are created by the manager and portable fearless during compilation, exactly as for the html documentation. Every time the html is created/updated, so should the corresponding plain text file.

Report:
Compact plain-text companion doc generation added alongside the existing per-package HTML doc, on every compile, in both the manager GUI and portable-fearless build paths.

HtmlDocBuilder.complete() (Coordinator/src/docBuilder/HtmlDocBuilder.java) is the single place all per-package HTML doc generation happens, invoked from every package compile via Coordinator.backend(); it now also writes <pkg>.txt next to <pkg>.html from the same HtmlDocRenderer instance (new renderText() method, reusing all existing filtering/sorting/naming logic). BaseCacheBuilder.buildInto also copies base.txt into baseCache.

Docs location note: originally written into a StandardLibrary/CLAUDE.md file, which was WRONG (Claude-agent config must stay local, never committed to a shared repo -- Marco caught this in review). Corrected: that file was removed from FearlessLang/StandardLibrary#41, and the documentation lives instead in the local C:\data\fearlessBranch3\CLAUDE.md.

Verification: clean builds of Commons/Frontend/Coordinator; fast Coordinator suite 287/287; full suite including integrationTests 321/322 (1 pre-existing unrelated abort, a Windows filename-with-colon limitation). Manually inspected real output (base.txt ~377KB vs base.html's ~1.7MB; a fixture's hello.txt ~3.9KB vs hello.html's ~33KB) -- signatures, extends clauses, doc prose, examples, inherited-method provenance all render correctly, no HTML leaking through.

Left alone: the manager GUI's "Open docs" button still only opens .html (task asked for generation parity, not a UI change); StandardLibrary/fearlessStandalone/base.html (a manually-committed release snapshot with no automated regeneration pipeline) untouched; no full jpackage-based portable/manager build was run (slow, exercises unrelated OS packaging) -- the shared code path was verified via the test suite instead.

PRs (FearlessLang, via the marcoautomation fork):
- https://github.com/FearlessLang/Coordinator/pull/88 -- the code + tests
- https://github.com/FearlessLang/StandardLibrary/pull/41 -- closed, not merged: its only content was the wrongly-committed CLAUDE.md, and removing it left nothing to merge. The doc-location note lives in the local fearlessBranch3\CLAUDE.md instead.

---

Task:
As you can see, you have a dedicated email (can you find it in your skills?)
Are you able to use that email to send and read emails?
Are you able to send and receive/analyse attachments too?
Try to send me (marco.servetto@gmail.com) and email with an attachment (if you can).

Report:
Can the dedicated email send emails? Yes -- via the Gmail web UI, not via SMTP.

Direct SMTP with the plain account password does not work, verified empirically rather than assumed:
- smtp.gmail.com:465 (SMTP_SSL) -> 535-5.7.8 Username and Password not accepted... https://support.google.com/mail/?p=BadCredentials
- smtp.gmail.com:587 (STARTTLS) -> same 535/BadCredentials error
- imap.gmail.com:993 -> AUTHENTICATIONFAILED Invalid credentials

This is Google's standard, permanent rejection of direct password auth for mail protocols now that "less secure app access" is fully retired -- it fails the same way regardless of the password being correct. Fixing it would require either an App Password (needs 2-Step Verification turned on for the account first, generated through an interactive Google Account Security page) or a full OAuth2 client (a registered Google Cloud project plus a consent flow) -- real, human-interactive setup steps, not something scriptable unattended. Neither was pursued since the alternative below worked cleanly.

What actually worked: driving the real Brave browser (the same screenshot-plus-synthetic-click technique the chatgpt-image-gen skill uses for ChatGPT) against the genuine Gmail web UI, logged in as marcoautomation80@gmail.com. Login succeeded on the first attempt with no 2FA challenge, no CAPTCHA, no "suspicious sign-in" block. The Brave profile already had Marco's own personal Google account signed in -- "Add another account" was used to sign in as the dedicated account alongside it without opening or touching Marco's own inbox at any point.

Can it read emails? Yes. After logging in, the live inbox (174 messages, mostly GitHub notifications tied to the marcoautomation identity, plus Google's own "new sign-in" security alert) and the Sent folder were both browsable.

Can it send attachments? Yes -- confirmed, not assumed. A real email was composed and sent:
- To: marco.servetto@gmail.com
- Subject: "Capability test from Claude (marcoautomation80) with attachment"
- Body: explains it's an automated overnight capability test, sent via the Gmail web UI since direct SMTP is blocked.
- Attachment: claude_email_test_attachment.txt (1 KB), a short note identifying itself as the test attachment with a timestamp.

Gmail's "Message sent" confirmation appeared, and the Sent folder showed exactly this one message with the attachment chip attached.

Can it receive and analyse attachments? Yes -- verified two ways:
1. Opened the sent email from the Sent folder and used Gmail's built-in attachment preview -- the rendered text matched the original exactly.
2. Downloaded the attachment through the browser's native save dialog and read the resulting local file with a normal file-read tool -- content again matched exactly, confirming genuine programmatic access to attachment content after the fact, not just a visual preview.

Did it actually send Marco the requested email? Yes. It should be sitting in marco.servetto@gmail.com's inbox: subject "Capability test from Claude (marcoautomation80) with attachment", from marcoAutomation <marcoautomation80@gmail.com>, with the claude_email_test_attachment.txt attachment described above.

Bottom line / what didn't work: programmatic SMTP/IMAP with the plain password is blocked by Google outright -- a real, hard wall, not a bug on this end. Everything else asked (send, read, send-with-attachment, read-attachment-content) works today through the browser-UI path, with no manual/interactive step needed. The browser session for this account now persists in the shared Brave profile, so a future task can reuse it directly without repeating the login flow.

No code changes were made; nothing in the fearlessBranch3 repos was touched.

---

Task:
Consider this web based game
https://github.com/MarcoServetto/ZeroToHero/blob/main/src/assetsLeo/SwordAndShieldMulti.html
There is some issue on the peer to peer play over the internet connection.
If one player take a very long time to do their more, the other player is disconnected.
Overall, we should make this more resilient.
-There should be some hearth beat of both players, even if they are not the player of the turn. Ideally a graphical element can show it, like the crown pulsating bigger and smaller when the hearth beat is received from that player.
-If there is a disconnection, the browsers should try to keep the information about the current game alive and simply try to reconnect, using the same players name and room name.
Make a PR with the fixed version.

Report:
PR: https://github.com/MarcoServetto/ZeroToHero/pull/10 (merged) -- fixes the heartbeat/reconnect issue.

Root cause: SwordAndShieldMulti.html already had an app-level heartbeat -- both players broadcast a "hello" over the Trystero/WebRTC data channel every 2 seconds regardless of whose turn it is, and a silent peer was declared gone after 12 seconds (six missed beats). The actual bug: a backgrounded browser tab gets its timers throttled by the browser itself (Chrome clamps a hidden tab's timers to roughly once a minute after a few minutes hidden), independent of any real network problem. Whoever is not on turn commonly tabs away, and anyone thinking for "a very long time" is also likely to have their screen lock or lose focus. Either way their heartbeats stop arriving well before the old 12-second window, so the other, still-focused player's client concludes they left and reassigns/loses their seat -- even though both browsers are still open and nothing actually failed. A second, separate gap: once reassigned this way, there was no way back except a full page reload.

What was changed: raised the peer-gone timeout from 12s to 75s; the crown SVG now pulses on each seat's real heartbeat (own seat on send, opponent's on receipt); an immediate heartbeat fires on visibilitychange; a peer already given up on that resumes heartbeating under the same connection is resumed directly; the room name plus a per-browser random token are kept in localStorage so a reload/crash auto-rejoins the same room and the survivor hands the seat straight back by token match. No build step or new dependency; single commit, 51 insertions/14 deletions.

How it was actually tested: two independent, isolated headless Brave instances driven via the Chrome DevTools Protocol, joined to the same real Trystero room over the live internet (no mocking). Paired two real clients, confirmed a real move propagated peer to peer, confirmed the heartbeat pulse fired for both seats. Froze one browser's page for 25s (longer than the old 12s cutoff): the other side never marked them lost -- reproducing the reported bug and confirming the fix. Froze it for 90s (past the new 75s limit): the survivor correctly gave up at ~76s, then both sides self-reunited within ~2s on unfreeze. Fully reloaded the frozen browser (fresh JS heap, same localStorage, new Trystero peer id): it auto-rejoined with zero manual input and was reseated on its original seat in ~1.0s, game record matching.

Cut scope / caveats: no human-visible "your name" field was added (reconnection identity is a silent persisted token, matching what the task asked for); the mobile/OS-level "whole browser process discarded and restarted cold" case wasn't tested separately beyond what the reload test covers (same code path); a known, narrow race (a spectator being appointed the same vacant seat at the exact moment the original occupant's token-match reconnect also fires) was deliberately left alone, consistent with how the rest of the file already tolerates similar races, and can't occur at all in the common case (a private two-person room, no spectators).

---

Task:
Make an example fearless project working with the logs.
Check that the logs work from the manager gui and that they are actually displayed.
If you find problems you may have to fix bugs and add features to the standard library about logging.
Write a PR adding that example fearless to the integration tests.

Report:
Both PRs open against FearlessLang. Fix verified end-to-end through the actual manager GUI, and amended after Marco's review caught a real remaining bug (see below).

What logging support existed before: base.Log (file-backed singleton logger, already working, already displayed correctly by the manager GUI's existing Logs panel), base.UnitTestLog (same mechanism, internal), and base.InMemoryLog[T]/base.LogReader[T] (a documented generic in-memory logging API that was completely non-functional -- nothing had ever called it before this task).

What was found broken in InMemoryLog, and fixed: (1) .log was declared as a runtime-backed "Magic!" method with no Java backing at all -- every call crashed. (2) System.readLog handed each reader a brand new, disconnected empty list every call, so nothing logged could ever be read back. (3) The reader's "reject generic logs" guard was checking the wrong thing and always threw (confirmed empirically: the log literal's own runtime class is always a JVM anonymous class with an empty simple name, so the original o.getClass().getSimpleName().endsWith("$0") check could never pass for any log, generic or concrete).

First-pass fix (now amended per Marco's review): the initial fix checked o.getClass().getInterfaces()[0]'s name instead, which passed for this task's own concrete example but Marco flagged that this hadn't been verified against the actually-generic case the check exists to reject, and separately expected the backing store to be keyed by the log's own per-declaration identity rather than by instance -- both addressed: storage is now keyed by that same per-declaration interface Class (stable and unique per named log, matching a "one static field per concrete type" model without touching the compiler's codegen), and read/consume moved into the log itself (_readCopy/_consumeCopy) so the log owns synchronization on its own list instead of the reader borrowing and separately synchronizing a reference to it. The integration test's content check was also strengthened from three unrelated contains() calls to one exact assertEquals over the full sorted, timestamp-stripped message list (item order is genuinely non-deterministic -- .flow.forEach runs in parallel -- so exact string equality on the raw file isn't possible, but the full exact set/count now is).

How the manager GUI display was verified: built the manager with the fix, launched the real packaged app, registered the testLogging project, compiled and ran it (output showed "3 steps recorded"), expanded the Logs panel, and viewed the produced log file -- content matched the file on disk exactly. Coordinator/test/integrationTests/RunIntegration.java's new testLogging() asserts the same LogFiles.list() lookup the manager GUI panel itself uses.

Test suite: full rebuild + run after the amendment, 317/317 Coordinator tests passing (no new failures; the previously-known unrelated symlink-permission abort in RealSourceOracleWithZipDslTest is environment-specific and untouched by this change).

Also done: redeployed the portable + manager builds via deploy-fearless-pages so the public downloads carry the fix: https://marcoautomation.github.io/shared-docs/fearless/

PRs (FearlessLang):
- https://github.com/FearlessLang/StandardLibrary/pull/42 -- the fix + example project
- https://github.com/FearlessLang/Coordinator/pull/89 -- integration test wiring

---

Task:
Currently running unit tests on fearless logs in a format that is compatible with what the Eclipse default plugin for JUnit6 can recognize. Is there a way to make so that that output is wired in the tests we see when we run all the tests on coordinator, so that we actually see in eclipse all those passing tests? I understand we may have to get some compromise, fine, let see what is the best we can easily get. I'm not sure how to report about this task. a PR would not make sense since it is about the eclipse set up and not the code; possibly something on the github pages with explanation and a zip to download and apply?

Report:
Task: wire Fearless's existing JUnit-XML-shaped unit-test log into Eclipse's own JUnit view, so running "all tests" on Coordinator shows individual Fearless-level test results. Deliverable: a GitHub Pages writeup + downloadable zip, not a code PR (per the task).

What already existed: StandardLibrary/base/testing.fear already logs each individual Fearless-level check as a JUnit-report-shaped testcase element via base.UnitTestLog. Nothing previously consumed that format -- Coordinator's own Java tests run each Fearless project as one @Test method, so "all tests" showed exactly one node per project, never the individual checks inside (1734 of them).

The compromise chosen: Eclipse's JUnit view has a stock Import feature that loads a standard JUnit XML report as a full test-run session, no plugin needed. ~24 lines were added to RunIntegration.java to write that XML file automatically as a side effect of the existing tests; only the Eclipse-side Import is manual. A full custom JUnit TestEngine for a zero-click experience was deliberately not built -- that needs Fearless's test runner to separate "list the tests" from "run the tests," a materially bigger and riskier change than a one-night task should attempt blind.

Verified in a real portable Eclipse install (eclipse-java-2026-06-R-win32-x86_64.zip): imported both a passing and an intentionally-failing run -- testingStandardLibrary showed "Runs: 1734/1734 (11 skipped)" with every individual check as its own node; testUnitTests showed one correctly-red node with Eclipse's own Failure Trace pane showing the exact Fearless assertion failure and stack trace.

What could not be fully verified: Eclipse's live "Run As -> JUnit Test" runner hit a classpath/JRE issue (missing junit-platform-engine/launcher, then a trimmed-JRE ClassNotFoundException) in a scratch test project -- an artifact of that scratch setup, unrelated to the shipped Import mechanism, not chased to a fix, flagged honestly.

Published:
- Page: https://marcoautomation.github.io/shared-docs/fearless/eclipse-tests/
- Zip: fearless-eclipse-junit-tests.zip on that page -- the RunIntegration.java patch, a standalone FearlessJUnitXml.java usable on any Fearless project, and a README.
- Incidental small PR (allowed by the task): https://github.com/FearlessLang/Coordinator/pull/90 -- one file, test-only, 24 lines.

Session note: interrupted mid-verification by a platform rate limit, resumed via a coordinator message; on resume, actual git/Eclipse state was verified from scratch (found and cleaned up incidental Eclipse-project artifacts Eclipse had written into the live Coordinator working tree; none of that reached git) before finishing.

---

Task:
Make sure that the manager looks to the hard drive to refresh its GUIs only when the GUI is visible; if the GUI is hidden or closed, the virtual threads continuously looking to the hard drive should stop, and restart when the gui is visible again.

Report:
Mechanism: Coordinator/src/manager/ManagerGui.java gained a synchronized windowVisible boolean plus a real blocking gate -- awaitWindowVisible() calls wait() while hidden, setWindowVisible() calls notifyAll() -- so the tick-loop virtual thread genuinely parks with zero wake-ups while the window isn't visible, rather than polling with the work skipped. tick() calls awaitWindowVisible() before doing any per-tick work (the elapsed-time label, shown.recheckFreshness(), recheckNextInRotation()). setWindowVisible(true) is called from showManager() (covers startup, the tray's "Show manager" item, tray-icon click, and new-project-message arrival) and from a new windowDeiconified handler (restoring a minimized window directly). setWindowVisible(false) is called from the existing windowClosing handler (closing hides rather than exits) and a new windowIconified handler (minimizing). Manager.watchMessages, the separate worker watching for new "open project" messages, was deliberately left running regardless of visibility -- it's how the manager notices new work while hidden in the tray. Only ManagerGui.java changed: 9 insertions, 1 deletion.

Verified two ways. First, the fast suite (Coordinator/test/mainCoordinator/TestAllFrontendCoordinator.java): 288 passed, 0 failed, 1 aborted -- pre-existing and unrelated, present before this change too. Second, real GUI verification, since this behavior isn't exercised by the automated suite: rebuilt the actual fearlessManaged app-image, launched it, registered a real example project (01_hello_world), and drove it with genuine window/tray interactions while taking jcmd thread-dump snapshots. Visible: the tick-loop thread cycling normally. Minimized via a real ShowWindow(SW_MINIMIZE): the thread parked in Object.wait() at ManagerGui.awaitWindowVisible, confirmed byte-for-byte unchanged 6 seconds later. Restored via ShowWindow(SW_RESTORE), exercising windowDeiconified: resumed cycling immediately. Closed via a real click on the window's own close button, exercising windowClosing: parked again. Reopened via a real double-click on the actual system-tray icon, exercising showManager(): resumed again. The elapsed-time label and the registered project's state stayed correct across the whole cycle.

Incidental fix along the way: C:\data\fearless-gui-tests\build\Commons-boot.jar, a bootstrap jar used only to source-launch the deploy scripts, was stale relative to the just-aligned Commons repo and failed to compile ModularBuild.java; refreshed from the current Commons.jar. Local tooling state only, not part of the repo or the PR.

PR: https://github.com/FearlessLang/Coordinator/pull/97 -- confirmed open, based on main, targeting FearlessLang/Coordinator (the upstream parent, not the fork).

---

Task:
A loaded project currently can be in states: invalid content, not compiled (no cache), not compiled (cache out of date), compiled, compiled and running. The task asked to expand this to a data/code split -- data:invalid, code:invalid, data:valid idle, data:read-write, data:read-only, plus the same four code sub-states as before -- backed by a new on-disk Info-format metadata file (a simplified JSON: only Str/List/Obj, empty of any shape considered equal, safe-charset strings) that the manager reads/writes and exposes through an editable view with a validating commit button. Dead/invalid links between projects (a code project reading/editing a data or code project that is itself invalid) needed to propagate: an invalid data folder makes a dependent code folder invalid, but not the reverse. The task also asked to fully redesign how the manager persists state around this file, to document anything that still needs saving outside the Info and why, and to add a menu entry dumping whatever manager-tracked state is NOT represented in the Info.

Report:
All new code lives in Coordinator's managerData package: Info/InfoParser/InfoPrinter (a hand-rolled Str/List/Obj format with per-node source spans, kept as new Java rather than added to StandardLibrary's own base.Info, since the manager is a Swing app that has no reason to depend on a live Fearless runtime just to read its own config), Kind (the four-value enum: idle, code, dataReadOnly, dataReadWrite), EntrySchema (the single place deciding whether the registry file "is correct" -- syntax, known attributes, valid kind, no duplicate names/paths), LinkCheck (the separate, never-fatal live dead-link check for a code entry's reads/edits), and InfoData (replaces the old DiskData, backing ManagerData with the new projects.info plus a small separate activity.txt). Manager/ManagerGui/ManagerMain and several existing managerInfo/managerList/managerIcons files were updated to the new model.

Errors from the editable view's commit button reuse Commons' own metaParser.Message/Span/Frame -- the exact machinery the real Fearless parser/type-checker use -- rather than a parallel diagnostic format, so a bad commit renders pixel-for-pixel like a real Fearless parse error (file path, numbered source line, caret under the bad token, message). Alias validation reuses managerIcons.FolderName's existing folder-name rule rather than the compiler's TName.isPkgName (which wrongly forbids a leading underscore FolderName already allows); chasing this down surfaced a real latent bug in FolderName.makeUnique, whose "no collision" fast path returned a raw OS folder name without validating it at all -- a folder like "My Project" would have produced an unusable alias that crashed the manager on the next read. Fixed as part of this change. Dead-link checking is deliberately a separate, never-thrown-from code path from corruption checking, so one project's bad link cannot take down the whole manager on startup -- an earlier draft conflated the two and would have done exactly that, caught by reasoning before it shipped.

What stays outside the Info, and why: compiled/run timestamps moved to a new activity.txt keyed by path -- high-frequency, non-semantic, machine-only bookkeeping (akin to a file mtime) that nothing validates against and no human hand-edits, kept separate from a document meant to be hand-edited and validated, and deliberately tolerant of corruption (a bad line drops silently) since losing "last compiled" cosmetics is harmless. Everything else the task worried about (bytes/timestamps/cache freshness/structural validity/session/known mains) was already recomputed live before this change and still is; the new "Show raw project state..." menu entry prints exactly that set per project, confirmed live, including the empty-registry case. The reads/edits schema and dead-link semantics are implemented and validated, but the actual file-to-type codegen capability the task's example hints at ("the gui will infer the standard file name -> type id transformation") does not exist anywhere in the compiler yet and was correctly treated as out of scope for a persistence redesign.

Verified two ways: the fast suite (TestAllFrontendCoordinator.java), 331 passed/0 failed/1 pre-existing unrelated abort, both before and after every round of fixes; and a real interactive pass against the rebuilt fearlessManaged app-image covering fresh startup, both new menu entries, registering and re-kinding a project, a valid commit, an invalid commit (confirmed Fearless-parser-style error, disk unchanged), introducing then fixing a dead link (badge/Compile-blocking behavior live, without invoking the real compiler), and metadata surviving a code->idle->code round trip. That interactive pass also surfaced and fixed a second real bug: the Project menu's "Error report" item wasn't recomputing its enabled state when the shown project's validity changed live, only on reselection.

Left alone, explicitly: no migration from the old registered.txt format (a fresh manager starts with an empty registry; the task didn't ask for migration and this is new infrastructure); the Info printer doesn't preserve hand-written comments (every programmatic write reprints canonically); no file-to-type codegen (schema and validation only, see above).

PRs (FearlessLang, via the marcoautomation fork):
- https://github.com/FearlessLang/Coordinator/pull/98 -- Coordinator only; StandardLibrary, Frontend and Commons were untouched (verified clean) since nothing here needed a stdlib or compiler change.
Pages: https://marcoautomation.github.io/shared-docs/fearless/ -- redeployed and confirmed live at Coordinator commit 9530751.

Note for whoever reviews tonight's PRs: this PR and pull/97 (task 1, same night) both independently branched off upstream/main and both touch Coordinator's ManagerGui.java, so they will very likely conflict with each other at merge time -- neither was rebased onto the other, by design (each overnight task starts from a clean upstream/main).

---

Task:
- make a file with all the .java files of common and frontend, with a preface mentioning the path of the file. Exclude all the test files
- read that all-source file and find a bug. Think carefully about the code itself, do not rush to 'run some tests'; think about the logic.
Then add a test verifying the found bug and mention this in the github pages.

Report:
Concatenated all non-test .java files from Commons/src (59 files) and Frontend/FearlessFrontend/src (78 files) -- 137 files, 13265 lines -- into one marked-up file and read it end to end reasoning about the logic, rather than pattern-matching or running the suite first.

The bug: Commons/src/metaParser/MetaParser.java, method parseBack (the back-scanning mirror of parseFront). parseFront correctly does its bookkeeping using only sub-parser-relative deltas; parseBack was supposed to mirror that using limit() but instead read the outer, absolute this.limit field in three spots where it needed the locally-captured, slice-relative start value. Concretely: if index has already advanced past 0 before parseBack is called (an ordinary mid-parse state), slice.subList(end+drop, limit) throws IndexOutOfBoundsException because limit exceeds the slice's size; even at index==0 the final this.limit += end sets the limit past the token list's actual size instead of shrinking it, corrupting the cursor. A tell: parseBack's doc comment is a verbatim copy of parseFront's ("forward"->"backwards" in one line) and still says it parses from the *start* of the parser, unchanged from the copy-paste. parseBack currently has zero callers anywhere across all four repos -- unused, unexercised library code, presumably why this survived.

Fix: a 3-line change using the locally-captured start instead of this.limit, and correctly shrinking the outer limit to index + end (mirroring how parseFront does this.index += end).

Regression test added to Frontend/FearlessFrontend/test/fearlessParser/MiscTests.java (Commons itself has no test source tree) -- builds a 6-token parser, advances index to 2, calls parseBack, and asserts the exact resulting split and exact parser state afterward. Verified the test actually catches the bug by reverting the fix and rerunning: failed with the predicted IndexOutOfBoundsException at the predicted line; reapplied the fix, passed. Fast suites green with the fix in place: TestAllFrontend.java 713/713, TestAllFrontendCoordinator.java 288/288 (1 unrelated pre-existing abort).

Outcome: FearlessLang/Commons#24 (the fix) was opened, then closed without merging by the project owner about 4.5 minutes later with no comment. Since the regression test in FearlessLang/Frontend#36 only makes sense together with #24, it was closed too, with an explanatory comment. Neither change landed. Given parseBack has no live callers today, this is plausibly a deliberate call that dead code isn't worth a fix commit, rather than disagreement that it's a real bug -- but no reason was given, so that reading is inference, not confirmed fact.

Published https://marcoautomation.github.io/shared-docs/bug-reports/metaparser-parseback-index.html (linked from the site's index), revised after the closures to accurately say "fix proposed, closed without merging" rather than claiming it landed.
