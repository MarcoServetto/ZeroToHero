Task:
Zero to hero game, Direct instructions.
Direct instructions:
Remove the error screen, disable the green button when the player can not go on (that is, when today you get the error screen instead), and make it glow green when enabled again. (many things glow green, follow the same set up)

Hardcode that the very first page of 101 has the green button glowing.
Build the game and open the browser to the local html file to confirm it is working as expected.

Open a Pr with your result.

Task:
Building on the last Zero to Hero Pr

Hard code that in the very first code entry point of level 101 we need to also have an 'example' button, when pressed it type exactly what you see in the yellow slide with 'autotyping', then a 'cartoonish' cursor go and presses green (do not actually press it; after that the screen go back to before the example button was pressed)

Build the game and open the browser to the local html file to confirm it is working as expected.

Amend the currently opened Pr with your result.

Task:
Building on the last Zero to Hero Pr

Add some ways so that multiple answers can count as the correct one. Consider where we can take advantage of this; often cases where stuff can be in many different orders.

Decide how the embedded domain specific language (EDS) in Java that works as the level editor for DirectInstructions should be updated to allow talking about multiple valid answers.

Look to the current level designs using DirectInstructions and see when multiple correct answers make sense and include them.

Build the game and open the browser to the local html file to confirm it is working as expected.

Amend the currently opened Pr with your result.

Task:
Building on the last Zero to Hero Pr

Can we have that some parts of text are 'protected' and not editable? in this way we can guide the user to not touch some part of the text.

Decide how the embedded domain specific language (EDS) in Java that works as the level editor for DirectInstructions should be updated to allow some text to be protected; in many other cases we used some special symbols inside the strings used; see if you can use a similar approach.

Look to the current level designs using DirectInstructions and see when protected text make sense and include them. This is mostly important when we ask the user to complete the code in steps, we should protect the code of the steps AFTER the current one, or users may accidentally do 'more' then intended and get stuck and waste work.

Build the game and open the browser to the local html file to confirm it is working as expected.

Amend the currently opened Pr with your result.

Task:
Building on the last Zero to Hero Pr

If the hint bunny is visible and next page is pressed, the hint bunny should disappear and reset

when pressing the hint button, the solution text should be shown for a little longer, like 1.5 times longer

Amend the currently opened Pr with your result.

Task:
Building on the last Zero to Hero Pr

When the answer is correct and glowing green, the hint bunny should not be showing. The player is just checking what they learned, we should give me quite and time.

When we go over a quiz in direct instructions, the test should then be 'locked in' and not editable any more.

Amend the currently opened Pr with your result.


Task:
Building on the last Zero to Hero Pr
Review all the work that has been done, check if there are some mistakes or anything to improve upon.

Amend the currently opened Pr with your result.
