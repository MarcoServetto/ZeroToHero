Use Opus 5.5 high subagents to handle those tasks.

Task series:
This is a different kind of overnight task, where you have to set up tasks yourself and coordinate (one agent at a time) to do a whole lot of overnight work.
You have more then 8 hours and plenty of tokens; so pace your progress.
Do not attempt a instant grand plan that will necessarily fail contact with reality. Make speculations, relax, think about options.

The whole code in
https://github.com/FearlessLang/Controllers
is AI generated and not really human reviewed.
You are now a newer stronger model.
Try to design a set of tasks to improve/fully rewrite Controllers.
As you can see, we are putting design documents in zerotohero.

As part of your tasks, make a design document to document 'what' controller does/should do; keep it high level, not 'how' it does or 'why', just 'what'.

It is possible that some corner cases in the behaviour were just accidents/misunderstandings, consider what could be a better semantic in case the semantic seems odd.

Make various PRs to the needed repositories. Amend existing ones when possible.

You have more then 8 hours and plenty of tokens to improve

- the conceptual behaviour and role of controllers. Remember Fearless aims to be truly portable as in fighting system conventions to enforce the same exact behaviour everywhere, not as in 'adapting to the system conventions smoothly'.
- the code of controllers: code should be compact and self consistent, including consistent with the code of frontend (the highest quality code we have and the 'example' when needed)
- the predictability/fail fast nature of controllers. Remember graceful degradation is the enemy here.

This is a 'reorganisation/code clean up step' before we apply proposals like this one
https://github.com/MarcoServetto/ZeroToHero/blob/main/tasks/OpenWithDesign
and before we start working toward expanding the set of object capabilities under System, where the manager will use the information about readable/writable files to allow more file system operations.