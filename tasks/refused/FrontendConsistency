Converging null checks (Objects.requireNonNull, Require.nonNull) onto plain assert.
Reason: all three are offensive checks, failing immediately on a bad input; they have different roles (requireNonNull returns the value, Require.nonNull checks several values together) and none of them is defensive programming.
