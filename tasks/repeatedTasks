Repeated overnight tasks. Each block is a whole task, to copy into LongHorizonTasks.txt.
------------------
Task "FrontendSimplification":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Frontend, stop and report it.
- Otherwise, in your working copy of Frontend, create the branch
  FrontendSimplification from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy FearlessFrontend/src of the branch into a new empty folder <folder> in your
   scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/FrontendSimplification (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   Identify one specific kind of code that can be simplified: not one place,
   but a kind of code that can be simplified the same way wherever it appears.
   Simplification often reduces the code size; a clearer shape of mostly the same size
   is good too. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the kind and a few places, before and after."""
2. Delete the folder. Decide if the kind makes the code better.
   Record it in ZeroToHero/tasks/ideas/FrontendSimplification: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch FrontendSimplification of Frontend, read all the code in
   FearlessFrontend/src and apply this simplification wherever it fits: <kind>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/FrontendSimplification into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs callers in Coordinator, Controllers or StandardLibrary to
   change too, change them on a branch FrontendSimplification of that repository.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinator.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch FrontendSimplification.
Stop when the diff of the Frontend PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Frontend, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/FrontendSimplification, and the
clickable links to all the opened PRs.
------------------
Task "FrontendConsistency":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Frontend, stop and report it.
- Otherwise, in your working copy of Frontend, create the branch
  FrontendConsistency from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy FearlessFrontend/src of the branch into a new empty folder <folder> in your
   scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/FrontendConsistency (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   Identify one specific way the code is not consistent: the same thing done in
   different ways. Not one place, but a kind of inconsistency, wherever it appears.
   Remember: long 'if' conditions should be replaced with a local variable,
   initialized with the long condition,  with a good name explaining the condition meaning.
   Code contracted against this rule should be re expandaded.
   Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the ways, the one the code should converge to
   and why, and a few places, before and after."""
2. Delete the folder. Decide if the direction makes the code better.
   Record it in ZeroToHero/tasks/ideas/FrontendConsistency: one line per direction,
   marked applied or refused, with how many times it was proposed; a direction already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch FrontendConsistency of Frontend, read all the code in
   FearlessFrontend/src and make it consistent in this way wherever it applies: <direction>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/FrontendConsistency into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Remember: long 'if' conditions should be replaced with a local variable,
   initialized with the long condition,  with a good name explaining the condition meaning.
   Code contracted against this rule should be re expandaded.
   If a change needs callers in Coordinator, Controllers or StandardLibrary to
   change too, change them on a branch FrontendConsistency of that repository.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinator.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch FrontendConsistency.
Stop when the diff of the Frontend PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Frontend, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/FrontendConsistency, and the
clickable links to all the opened PRs.
------------------
Task "FrontendBugs":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Frontend, stop and report it.
- Otherwise, in your working copy of Frontend, create the branch
  FrontendBugs from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontend.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy FearlessFrontend/src of the branch into a new empty folder <folder> in your
   scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/FrontendBugs (as <folder>/ruledOut) and, when it exists,
   C:\data\fearlessPaper\Fearless_TOPLAS without its git data (as <folder>/paper).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   <folder>/paper, when present, is a paper whose type system describes the same
   language as <folder>/src/typeSystem.
   Identify one kind of bug: a mistake that is likely to happen in several places,
   not one place. Think about the logic of the code.
   The behavior marked Good in <folder>/ruledOut is intended.
   Do not edit anything. Reply with the kind of bug and one concrete Fearless
   program with the wrong result it produces: a wrong error, a missing error, or a crash."""
2. Delete the folder. Compile the program yourself and decide if the bug is real.
   Record it in ZeroToHero/tasks/ideas/FrontendBugs: one line per kind of bug,
   marked reported or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch FrontendBugs of Frontend, read all the code in
   FearlessFrontend/src and find every place with this kind of bug: <kind>.
   For each, add a failing test showing it, in the style of the existing tests;
   do not fix the bugs. If you meet other bugs, add failing tests for them too.
   The behavior marked Good in ZeroToHero/tasks/ruledOut/FrontendBugs is intended.
   Run TestAllFrontend.java: only these tests and the new ones may fail: <list>.
   Commit and push. Reply with the names of the new tests."""
4. Read the new tests. Remove those that do not fail for the stated reason, commit and push.
Stop when the diff of the Frontend PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for Frontend, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/FrontendBugs, and the
clickable links to all the opened PRs.
------------------
Task "CommonsSimplification":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Commons, stop and report it.
- Otherwise, in your working copy of Commons, create the branch
  CommonsSimplification from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Commons/src of the branch into a new empty folder <folder> in your
   scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/CommonsSimplification (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   Identify one specific kind of code that can be simplified: not one place,
   but a kind of code that can be simplified the same way wherever it appears.
   Simplification often reduces the code size; a clearer shape of mostly the same size
   is good too. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the kind and a few places, before and after."""
2. Delete the folder. Decide if the kind makes the code better.
   Record it in ZeroToHero/tasks/ideas/CommonsSimplification: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CommonsSimplification of Commons, read all the code in
   Commons/src and apply this simplification wherever it fits: <kind>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/CommonsSimplification into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs callers in Frontend, Coordinator, Controllers or StandardLibrary to
   change too, change them on a branch CommonsSimplification of that repository.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Copy the regenerated out/modular/mods/Commons.jar over Commons/Commons.jar.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinator.java, copy Commons.jar again, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch CommonsSimplification.
Stop when the diff of the Commons PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Commons, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/CommonsSimplification, and the
clickable links to all the opened PRs.
------------------
Task "CommonsConsistency":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Commons, stop and report it.
- Otherwise, in your working copy of Commons, create the branch
  CommonsConsistency from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Commons/src of the branch into a new empty folder <folder> in your
   scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/CommonsConsistency (as <folder>/ruledOut) and
   Frontend/FearlessFrontend/src (as <folder>/reference).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   Identify one specific way the code is not consistent: the same thing done in
   different ways. Not one place, but a kind of inconsistency, wherever it appears.
   When the code in <folder>/reference does the same thing, its way is the one to
   converge to. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the ways, the one the code should converge to
   and why, and a few places, before and after."""
2. Delete the folder. Decide if the direction makes the code better.
   Record it in ZeroToHero/tasks/ideas/CommonsConsistency: one line per direction,
   marked applied or refused, with how many times it was proposed; a direction already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CommonsConsistency of Commons, read all the code in
   Commons/src and make it consistent in this way wherever it applies: <direction>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/CommonsConsistency into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs callers in Frontend, Coordinator, Controllers or StandardLibrary to
   change too, change them on a branch CommonsConsistency of that repository.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Copy the regenerated out/modular/mods/Commons.jar over Commons/Commons.jar.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinator.java, copy Commons.jar again, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch CommonsConsistency.
Stop when the diff of the Commons PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Commons, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/CommonsConsistency, and the
clickable links to all the opened PRs.
------------------
Task "CommonsBugs":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Commons, stop and report it.
- Otherwise, in your working copy of Commons, create the branch
  CommonsBugs from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Commons/src of the branch into a new empty folder <folder> in your
   scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/CommonsBugs (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   Identify one kind of bug: a mistake that is likely to happen in several places,
   not one place. Think about the logic of the code.
   The behavior marked Good in <folder>/ruledOut is intended.
   Do not edit anything. Reply with the kind of bug and one concrete input
   with the wrong result it produces."""
2. Delete the folder. Run the input yourself and decide if the bug is real.
   Record it in ZeroToHero/tasks/ideas/CommonsBugs: one line per kind of bug,
   marked reported or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CommonsBugs of Commons, read all the code in
   Commons/src and find every place with this kind of bug: <kind>.
   For each, add a failing test showing it, in the style of the existing tests;
   do not fix the bugs. If you meet other bugs, add failing tests for them too.
   The behavior marked Good in ZeroToHero/tasks/ruledOut/CommonsBugs is intended.
   Run TestAllFrontendCoordinator.java: only these tests and the new ones may fail: <list>.
   Commit and push. Reply with the names of the new tests."""
4. Read the new tests. Remove those that do not fail for the stated reason, commit and push.
Stop when the diff of the Commons PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for Commons, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/CommonsBugs, and the
clickable links to all the opened PRs.
------------------
Task "CoordinatorSimplification":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Coordinator, stop and report it.
- Otherwise, in your working copy of Coordinator, create the branch
  CoordinatorSimplification from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Coordinator/src and Coordinator/Build/src of the branch into a new empty folder
   <folder> in your scratchpad (as <folder>/src and <folder>/build), without any git data,
   together with ZeroToHero/tasks/ruledOut/CoordinatorSimplification (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src and <folder>/build.
   Identify one specific kind of code that can be simplified: not one place,
   but a kind of code that can be simplified the same way wherever it appears.
   Simplification often reduces the code size; a clearer shape of mostly the same size
   is good too. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the kind and a few places, before and after."""
2. Delete the folder. Decide if the kind makes the code better.
   Record it in ZeroToHero/tasks/ideas/CoordinatorSimplification: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CoordinatorSimplification of Coordinator, read all the code in
   src and Build/src and apply this simplification wherever it fits: <kind>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/CoordinatorSimplification into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs callers in Controllers or StandardLibrary to
   change too, change them on a branch CoordinatorSimplification of that repository.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinator.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch CoordinatorSimplification.
Stop when the diff of the Coordinator PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Coordinator, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/CoordinatorSimplification, and the
clickable links to all the opened PRs.
------------------
Task "CoordinatorConsistency":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Coordinator, stop and report it.
- Otherwise, in your working copy of Coordinator, create the branch
  CoordinatorConsistency from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Coordinator/src and Coordinator/Build/src of the branch into a new empty folder
   <folder> in your scratchpad (as <folder>/src and <folder>/build), without any git data,
   together with ZeroToHero/tasks/ruledOut/CoordinatorConsistency (as <folder>/ruledOut) and
   Frontend/FearlessFrontend/src (as <folder>/reference).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src and <folder>/build.
   Identify one specific way the code is not consistent: the same thing done in
   different ways. Not one place, but a kind of inconsistency, wherever it appears.
   When the code in <folder>/reference does the same thing, its way is the one to
   converge to. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the ways, the one the code should converge to
   and why, and a few places, before and after."""
2. Delete the folder. Decide if the direction makes the code better.
   Record it in ZeroToHero/tasks/ideas/CoordinatorConsistency: one line per direction,
   marked applied or refused, with how many times it was proposed; a direction already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CoordinatorConsistency of Coordinator, read all the code in
   src and Build/src and make it consistent in this way wherever it applies: <direction>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/CoordinatorConsistency into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs callers in Controllers or StandardLibrary to
   change too, change them on a branch CoordinatorConsistency of that repository.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinator.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch CoordinatorConsistency.
Stop when the diff of the Coordinator PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Coordinator, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/CoordinatorConsistency, and the
clickable links to all the opened PRs.
------------------
Task "CoordinatorBugs":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Coordinator, stop and report it.
- Otherwise, in your working copy of Coordinator, create the branch
  CoordinatorBugs from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Coordinator/src and Coordinator/Build/src of the branch into a new empty folder
   <folder> in your scratchpad (as <folder>/src and <folder>/build), without any git data,
   together with ZeroToHero/tasks/ruledOut/CoordinatorBugs (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src and <folder>/build.
   Identify one kind of bug: a mistake that is likely to happen in several places,
   not one place. Think about the logic of the code.
   The behavior marked Good in <folder>/ruledOut is intended.
   Do not edit anything. Reply with the kind of bug and one concrete Fearless
   project with the wrong result it produces."""
2. Delete the folder. Build the project yourself and decide if the bug is real.
   Record it in ZeroToHero/tasks/ideas/CoordinatorBugs: one line per kind of bug,
   marked reported or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CoordinatorBugs of Coordinator, read all the code in
   src and Build/src and find every place with this kind of bug: <kind>.
   For each, add a failing test showing it, in the style of the existing tests;
   do not fix the bugs. If you meet other bugs, add failing tests for them too.
   The behavior marked Good in ZeroToHero/tasks/ruledOut/CoordinatorBugs is intended.
   Run TestAllFrontendCoordinator.java: only these tests and the new ones may fail: <list>.
   Commit and push. Reply with the names of the new tests."""
4. Read the new tests. Remove those that do not fail for the stated reason, commit and push.
Stop when the diff of the Coordinator PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for Coordinator, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/CoordinatorBugs, and the
clickable links to all the opened PRs.
------------------
Task "CoordinatorErrorMessages":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Coordinator, stop and report it.
- Otherwise, in your working copy of Coordinator, create the branch
  CoordinatorErrorMessages from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinator.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Coordinator/src and Coordinator/Build/src of the branch into a new empty folder
   <folder> in your scratchpad (as <folder>/src and <folder>/build), without any git data,
   together with ZeroToHero/tasks/ruledOut/CoordinatorErrorMessages (as <folder>/ruledOut),
   Frontend/FearlessFrontend/src (as <folder>/reference), and the section
   "Names, messages, tests" of the global CLAUDE.md (as <folder>/wording).
   Start an inner agent:
   """Read only files inside <folder>. Read the error messages a user of Fearless can get
   from the code in <folder>/src and <folder>/build. The error messages of the code in
   <folder>/reference are the model, and <folder>/wording has the rules for wording them.
   Identify one kind of defect in the messages of <folder>/src and <folder>/build:
   not one message, but a kind of defect, wherever it appears.
   Never rewrite a message marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good message as it is.
   Do not edit anything. Reply with the kind and a few messages, before and after."""
2. Delete the folder. Decide if the kind makes the messages better.
   Record it in ZeroToHero/tasks/ideas/CoordinatorErrorMessages: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch CoordinatorErrorMessages of Coordinator, fix every error message
   in src and Build/src with this defect: <kind>, and update the expected texts of the tests.
   The error messages of Frontend are the model; the section "Names, messages, tests"
   of the global CLAUDE.md has the rules for wording them. Where the right wording
   is unclear, put <HELP ME WITH WORDING> in the message.
   Improve any other message that is clearly improvable. Never rewrite a message
   marked Good in ZeroToHero/tasks/ruledOut/CoordinatorErrorMessages into its Bad
   form; where no Bad form is given, leave the Good message as it is.
   If a change needs the expected texts in Controllers to change too, change them
   on a branch CoordinatorErrorMessages of Controllers.
   Run TestAllFrontendCoordinator.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good messages of the ruled out file into Bad ones, rerun TestAllFrontendCoordinator.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch CoordinatorErrorMessages.
Stop when the diff of the Coordinator PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
At the end run TestAllFrontendCoordinatorIntegration.java and TestAllController.java.
Make sure at the end there is a PR for Coordinator, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/CoordinatorErrorMessages, and the
clickable links to all the opened PRs.
------------------
Task "ControllersSimplification":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Controllers, stop and report it.
- Otherwise, in your working copy of Controllers, create the branch
  ControllersSimplification from upstream main, push an empty commit, and open the PR.
- Run TestAllController.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Controllers/src of the branch, without src/agentTools, into a new empty folder
   <folder> in your scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/ControllersSimplification (as <folder>/ruledOut) and
   ZeroToHero/tasks/ControllersDesign (as <folder>/design).
   Start an inner agent:
   """Read only files inside <folder>. <folder>/design says what the program does.
   Read all the code in <folder>/src.
   Identify one specific kind of code that can be simplified: not one place,
   but a kind of code that can be simplified the same way wherever it appears.
   Simplification often reduces the code size; a clearer shape of mostly the same size
   is good too. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the kind and a few places, before and after."""
2. Delete the folder. Decide if the kind makes the code better.
   Record it in ZeroToHero/tasks/ideas/ControllersSimplification: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch ControllersSimplification of Controllers, read all the code in
   src, except src/agentTools, and apply this simplification wherever it fits: <kind>.
   ZeroToHero/tasks/ControllersDesign says what the program does.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/ControllersSimplification into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Run TestAllController.java: only these tests may fail: <list>.
   Commit and push. Reply with the test counts."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllController.java, commit and push.
Stop when the diff of the Controllers PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for Controllers, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/ControllersSimplification, and the
clickable links to all the opened PRs.
------------------
Task "ControllersConsistency":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Controllers, stop and report it.
- Otherwise, in your working copy of Controllers, create the branch
  ControllersConsistency from upstream main, push an empty commit, and open the PR.
- Run TestAllController.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Controllers/src of the branch, without src/agentTools, into a new empty folder
   <folder> in your scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/ControllersConsistency (as <folder>/ruledOut) and
   Frontend/FearlessFrontend/src (as <folder>/reference).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/src.
   Identify one specific way the code is not consistent: the same thing done in
   different ways. Not one place, but a kind of inconsistency, wherever it appears.
   When the code in <folder>/reference does the same thing, its way is the one to
   converge to. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the ways, the one the code should converge to
   and why, and a few places, before and after."""
2. Delete the folder. Decide if the direction makes the code better.
   Record it in ZeroToHero/tasks/ideas/ControllersConsistency: one line per direction,
   marked applied or refused, with how many times it was proposed; a direction already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch ControllersConsistency of Controllers, read all the code in
   src, except src/agentTools, and make it consistent in this way wherever it applies: <direction>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/ControllersConsistency into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Run TestAllController.java: only these tests may fail: <list>.
   Commit and push. Reply with the test counts."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllController.java, commit and push.
Stop when the diff of the Controllers PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for Controllers, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/ControllersConsistency, and the
clickable links to all the opened PRs.
------------------
Task "ControllersBugs":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/Controllers, stop and report it.
- Otherwise, in your working copy of Controllers, create the branch
  ControllersBugs from upstream main, push an empty commit, and open the PR.
- Run TestAllController.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy Controllers/src of the branch, without src/agentTools, into a new empty folder
   <folder> in your scratchpad, without any git data, together with
   ZeroToHero/tasks/ruledOut/ControllersBugs (as <folder>/ruledOut) and
   ZeroToHero/tasks/ControllersDesign (as <folder>/design).
   Start an inner agent:
   """Read only files inside <folder>. <folder>/design says what the program does:
   code doing something else is a bug. Read all the code in <folder>/src.
   Identify one kind of bug: a mistake that is likely to happen in several places,
   not one place. Think about the logic of the code.
   The behavior marked Good in <folder>/ruledOut is intended.
   Do not edit anything. Reply with the kind of bug and one concrete sequence
   of actions with the wrong result it produces."""
2. Delete the folder. Check the sequence yourself, by reading the code or with a headless test,
   and decide if the bug is real.
   Record it in ZeroToHero/tasks/ideas/ControllersBugs: one line per kind of bug,
   marked reported or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch ControllersBugs of Controllers, read all the code in
   src, except src/agentTools, and find every place with this kind of bug: <kind>.
   ZeroToHero/tasks/ControllersDesign says what the program does.
   For each, add a failing headless test showing it, in the style of the existing tests;
   do not fix the bugs. If you meet other bugs, add failing tests for them too.
   The behavior marked Good in ZeroToHero/tasks/ruledOut/ControllersBugs is intended.
   Run TestAllController.java: only these tests and the new ones may fail: <list>.
   Commit and push. Reply with the names of the new tests."""
4. Read the new tests. Remove those that do not fail for the stated reason, commit and push.
Stop when the diff of the Controllers PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for Controllers, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/ControllersBugs, and the
clickable links to all the opened PRs.
------------------
Task "StandardLibraryGuiSimplification":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/StandardLibrary, stop and report it.
- Otherwise, in your working copy of StandardLibrary, create the branch
  StandardLibraryGuiSimplification from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy StandardLibrary/base/gui and StandardLibrary/rt/gui of the branch into a new empty
   folder <folder> in your scratchpad (as <folder>/base and <folder>/rt), without any git data,
   together with ZeroToHero/tasks/ruledOut/StandardLibraryGuiSimplification (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/base and <folder>/rt:
   a GUI library written in Fearless, and the Java code it runs on.
   Identify one specific kind of code that can be simplified: not one place,
   but a kind of code that can be simplified the same way wherever it appears.
   Simplification often reduces the code size; a clearer shape of mostly the same size
   is good too. Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the kind and a few places, before and after."""
2. Delete the folder. Decide if the kind makes the code better.
   Record it in ZeroToHero/tasks/ideas/StandardLibraryGuiSimplification: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch StandardLibraryGuiSimplification of StandardLibrary, read all the code in
   base/gui and rt/gui and apply this simplification wherever it fits: <kind>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/StandardLibraryGuiSimplification into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs code in FearlessTour or ZeroToHero to change too, change it
   on a branch StandardLibraryGuiSimplification of that repository.
   Run TestAllFrontendCoordinatorIntegration.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinatorIntegration.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch StandardLibraryGuiSimplification.
Stop when the diff of the StandardLibrary PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for StandardLibrary, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/StandardLibraryGuiSimplification, and the
clickable links to all the opened PRs.
------------------
Task "StandardLibraryGuiConsistency":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/StandardLibrary, stop and report it.
- Otherwise, in your working copy of StandardLibrary, create the branch
  StandardLibraryGuiConsistency from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy StandardLibrary/base/gui and StandardLibrary/rt/gui of the branch into a new empty
   folder <folder> in your scratchpad (as <folder>/base and <folder>/rt), without any git data,
   together with ZeroToHero/tasks/ruledOut/StandardLibraryGuiConsistency (as <folder>/ruledOut) and
   Frontend/FearlessFrontend/src (as <folder>/reference).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/base and <folder>/rt:
   a GUI library written in Fearless, and the Java code it runs on.
   Identify one specific way the code is not consistent: the same thing done in
   different ways, or a family of builders or widgets where some members offer a method,
   a name or an argument order that the others do not. Not one place, but a kind of
   inconsistency, wherever it appears. For the Java code, when the code in
   <folder>/reference does the same thing, its way is the one to converge to.
   Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the ways, the one the code should converge to
   and why, and a few places, before and after."""
2. Delete the folder. Decide if the direction makes the code better.
   Record it in ZeroToHero/tasks/ideas/StandardLibraryGuiConsistency: one line per direction,
   marked applied or refused, with how many times it was proposed; a direction already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch StandardLibraryGuiConsistency of StandardLibrary, read all the code in
   base/gui and rt/gui and make it consistent in this way wherever it applies: <direction>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/StandardLibraryGuiConsistency into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs code in FearlessTour or ZeroToHero to change too, change it
   on a branch StandardLibraryGuiConsistency of that repository.
   Run TestAllFrontendCoordinatorIntegration.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinatorIntegration.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch StandardLibraryGuiConsistency.
Stop when the diff of the StandardLibrary PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for StandardLibrary, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/StandardLibraryGuiConsistency, and the
clickable links to all the opened PRs.
------------------
Task "StandardLibraryGuiBugs":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/StandardLibrary, stop and report it.
- Otherwise, in your working copy of StandardLibrary, create the branch
  StandardLibraryGuiBugs from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy StandardLibrary/base/gui and StandardLibrary/rt/gui of the branch into a new empty
   folder <folder> in your scratchpad (as <folder>/base and <folder>/rt), without any git data,
   together with ZeroToHero/tasks/ruledOut/StandardLibraryGuiBugs (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/base and <folder>/rt:
   a GUI library written in Fearless, and the Java code it runs on.
   Identify one kind of bug: a mistake that is likely to happen in several places,
   not one place. Hand trace concrete window geometry and event sequences.
   The behavior marked Good in <folder>/ruledOut is intended.
   Do not edit anything. Reply with the kind of bug and one concrete program or
   sequence of user actions with the wrong result it produces."""
2. Delete the folder. Check it yourself, by hand tracing or by running the program
   with DeployPortableFearless.java, and decide if the bug is real.
   Record it in ZeroToHero/tasks/ideas/StandardLibraryGuiBugs: one line per kind of bug,
   marked reported or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch StandardLibraryGuiBugs of StandardLibrary, read all the code in
   base/gui and rt/gui and find every place with this kind of bug: <kind>.
   For each, add a test showing it to the GUI integration tests, and fix the bug
   when the fix is small; otherwise leave the test failing. If you meet other bugs,
   do the same for them.
   The behavior marked Good in ZeroToHero/tasks/ruledOut/StandardLibraryGuiBugs is intended.
   Run TestAllFrontendCoordinatorIntegration.java: only these tests and the new unfixed
   ones may fail: <list>. Commit and push. Reply with the names of the new tests and
   which bugs are fixed."""
4. Read the diff and the new tests. Remove the tests that do not show the stated bug,
   rerun TestAllFrontendCoordinatorIntegration.java, commit and push.
Stop when the diff of the StandardLibrary PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for StandardLibrary, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/StandardLibraryGuiBugs, and the
clickable links to all the opened PRs.
------------------
Task "StandardLibraryBaseConsistency":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.
The code of this task is StandardLibrary/base and StandardLibrary/rt, without the GUI
(base/gui, rt/gui) and without the flows (base/datatypes/collections/flows, rt/Flows*).

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/StandardLibrary, stop and report it.
- Otherwise, in your working copy of StandardLibrary, create the branch
  StandardLibraryBaseConsistency from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy the code of this task from the branch into a new empty folder <folder> in your
   scratchpad (as <folder>/base and <folder>/rt), without any git data, together with
   ZeroToHero/tasks/ruledOut/StandardLibraryBaseConsistency (as <folder>/ruledOut) and
   StandardLibrary/dbgOut/baseCache/base.txt (as <folder>/base.txt).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/base and <folder>/rt:
   the standard library of Fearless, and the Java code it runs on. <folder>/base.txt
   is its API. Identify one specific way the API is not consistent: the same operation
   with different names, argument orders or shapes on different types, a family of
   methods that some types offer and others do not, a convention followed by most types
   and not by others. Not one place, but a kind of inconsistency, wherever it appears.
   Never rewrite code marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   Do not edit anything. Reply with the ways, the one the code should converge to
   and why, and a few places, before and after."""
2. Delete the folder. Decide if the direction makes the API better.
   Record it in ZeroToHero/tasks/ideas/StandardLibraryBaseConsistency: one line per direction,
   marked applied or refused, with how many times it was proposed; a direction already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch StandardLibraryBaseConsistency of StandardLibrary, read all the code in
   base and rt, without base/gui, rt/gui, base/datatypes/collections/flows and rt/Flows*,
   and make it consistent in this way wherever it applies: <direction>.
   Improve anything else you see that is clearly improvable. Never rewrite code
   marked Good in ZeroToHero/tasks/ruledOut/StandardLibraryBaseConsistency into its Bad
   form; where no Bad form is given, leave the Good code as it is.
   If a change needs code in FearlessTour or ZeroToHero to change too, change it
   on a branch StandardLibraryBaseConsistency of that repository.
   Run TestAllFrontendCoordinatorIntegration.java: only these tests may fail: <list>.
   Commit and push in every repository you changed. Reply with the test counts
   and the repositories you changed."""
4. Read the diff of the commit. Revert what turns Good code of the ruled out file into Bad code, rerun TestAllFrontendCoordinatorIntegration.java, commit and push.
   For every other repository changed for the first time tonight, open its PR
   from the branch StandardLibraryBaseConsistency.
Stop when the diff of the StandardLibrary PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for StandardLibrary, one for ZeroToHero with the
ideas file and the done tasks and one for each other repository changed.

Report: the content of ZeroToHero/tasks/ideas/StandardLibraryBaseConsistency, and the
clickable links to all the opened PRs.
------------------
Task "StandardLibraryBaseBugs":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.
The code of this task is StandardLibrary/base and StandardLibrary/rt, without the GUI
(base/gui, rt/gui) and without the flows (base/datatypes/collections/flows, rt/Flows*).

Setup:
- If a PR of this task from an earlier night is still open on
  FearlessLang/StandardLibrary, stop and report it.
- Otherwise, in your working copy of StandardLibrary, create the branch
  StandardLibraryBaseBugs from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy the code of this task from the branch into a new empty folder <folder> in your
   scratchpad (as <folder>/base and <folder>/rt), without any git data, together with
   ZeroToHero/tasks/ruledOut/StandardLibraryBaseBugs (as <folder>/ruledOut).
   Start an inner agent:
   """Read only files inside <folder>. Read all the code in <folder>/base and <folder>/rt:
   the standard library of Fearless, and the Java code it runs on. A Fearless method
   whose body is replaced by a Java method in <folder>/rt never runs that body, so
   compare the two. A doc comment promising something the code does not do is a bug too.
   Identify one kind of bug: a mistake that is likely to happen in several places,
   not one place. Think about the logic of the code.
   The behavior marked Good in <folder>/ruledOut is intended.
   Do not edit anything. Reply with the kind of bug and one concrete Fearless
   expression with the wrong result it produces."""
2. Delete the folder. Run the expression yourself and decide if the bug is real.
   Record it in ZeroToHero/tasks/ideas/StandardLibraryBaseBugs: one line per kind of bug,
   marked reported or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch StandardLibraryBaseBugs of StandardLibrary, read all the code in
   base and rt, without base/gui, rt/gui, base/datatypes/collections/flows and rt/Flows*,
   and find every place with this kind of bug: <kind>.
   For each, add a failing check to integrationTests/testingStandardLibrary, in the style
   of the checks there; do not fix the bugs. If you meet other bugs, add failing checks
   for them too.
   The behavior marked Good in ZeroToHero/tasks/ruledOut/StandardLibraryBaseBugs is intended.
   Run TestAllFrontendCoordinatorIntegration.java: only these tests and the new ones may fail: <list>.
   Commit and push. Reply with the names of the new checks."""
4. Read the new checks. Remove those that do not fail for the stated reason, commit and push.
Stop when the diff of the StandardLibrary PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for StandardLibrary, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/StandardLibraryBaseBugs, and the
clickable links to all the opened PRs.
------------------
Task "FearlessTourExplanations":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.
The tests of FearlessTour are in src/markDownTests; there is no script: run
org.junit.platform.console.ConsoleLauncher on a classpath made of the jars listed in
externalJars/readme.txt, the Commons, Frontend and Coordinator jars, and the jars in
Coordinator/testJars.

Setup:
- If a PR of this task from an earlier night is still open on
  MarcoServetto/FearlessTour, stop and report it.
- Otherwise, in your working copy of FearlessTour, create the branch
  FearlessTourExplanations from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java, so that
  StandardLibrary/dbgOut/baseCache/base.txt describes the current standard library.
- Run the tests of FearlessTour and note the failing tests.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy FearlessTour/src/chaptersOfZeroToHero of the branch into a new empty folder <folder>
   in your scratchpad (as <folder>/chapters), without any git data, together with
   ZeroToHero/tasks/ruledOut/FearlessTourExplanations (as <folder>/ruledOut) and
   StandardLibrary/dbgOut/baseCache/base.txt (as <folder>/base.txt).
   Start an inner agent:
   """Read only files inside <folder>. Read all the chapters in <folder>/chapters: a guide
   to the Fearless language. <folder>/base.txt is the API of its standard library.
   Identify one kind of mistake in the explanations: not one sentence, but a kind of
   mistake, wherever it appears.
   Never rewrite text marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good text as it is.
   Do not edit anything. Reply with the kind and a few places, before and after."""
2. Delete the folder. Check against base.txt, or by compiling, that the places are wrong,
   and decide if the kind makes the guide better.
   Record it in ZeroToHero/tasks/ideas/FearlessTourExplanations: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch FearlessTourExplanations of FearlessTour, read all the chapters in
   src/chaptersOfZeroToHero and fix every place with this mistake: <kind>.
   StandardLibrary/dbgOut/baseCache/base.txt is the API of the standard library.
   Code shown in the text that the tests do not compile becomes a test: inside the
   text when that is simple, otherwise at the bottom of the chapter as other chapters do.
   Improve anything else you see that is clearly improvable. Never rewrite text
   marked Good in ZeroToHero/tasks/ruledOut/FearlessTourExplanations into its Bad
   form; where no Bad form is given, leave the Good text as it is.
   Run the tests of FearlessTour (<how to run them>): only these tests may fail: <list>.
   Commit and push. Reply with the test counts."""
4. Read the diff of the commit. Revert what turns Good text of the ruled out file into Bad text, rerun the tests of FearlessTour, commit and push.
Stop when the diff of the FearlessTour PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for FearlessTour, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/FearlessTourExplanations, and the
clickable links to all the opened PRs.
------------------
Task "ZeroToHeroPuzzles":
You are the outer agent; the sub agents you start are the inner agents.
Run at most one inner agent at a time. Every agent should be Opus 5.5 high.
Inner agents write no logs; this task overrides the global rule on sub agent logs.
Every inner agent starts in a fresh environment, with no trace of the activity of earlier inner agents.

Setup:
- If a PR of this task from an earlier night is still open on
  MarcoServetto/ZeroToHero, stop and report it.
- Otherwise, in your working copy of ZeroToHero, create the branch
  ZeroToHeroPuzzles from upstream main, push an empty commit, and open the PR.
- Run TestAllFrontendCoordinatorIntegration.java, so that
  StandardLibrary/dbgOut/baseCache/base.txt describes the current standard library.

Rounds:
Before each step, check the usage limits. Do not worry about getting near the weekly limit, all good.
If the current 5 hours session is over 80%, sleep until 5 minutes after the session resets,
as the overnight rules describe.
Do not start a new round after 8am.
1. Copy ZeroToHero/src/levels of the branch into a new empty folder <folder> in your
   scratchpad (as <folder>/levels), without any git data, together with
   ZeroToHero/tasks/ruledOut/ZeroToHeroPuzzles (as <folder>/ruledOut) and
   StandardLibrary/dbgOut/baseCache/base.txt (as <folder>/base.txt).
   Start an inner agent:
   """Read only files inside <folder>. Read all the levels in <folder>/levels: puzzles
   of a game teaching the Fearless language. <folder>/base.txt is the API of its standard
   library. The puzzles are not compiled, so they can disagree with the language.
   Identify one kind of mistake in the puzzles: not one puzzle, but a kind of mistake,
   wherever it appears.
   Never rewrite text marked Good in <folder>/ruledOut into its Bad
   form; where no Bad form is given, leave the Good text as it is.
   Do not edit anything. Reply with the kind and a few puzzles, before and after."""
2. Delete the folder. Check with the Frontend parser and type system that the puzzles are wrong,
   and decide if the kind makes the game better.
   Record it in ZeroToHero/tasks/ideas/ZeroToHeroPuzzles: one line per kind,
   marked applied or refused, with how many times it was proposed; a kind already
   in the file, even with different words, increases its count.
   If you refuse it, go back to 1. After 5 refusals in a row, stop.
3. Start an inner agent:
   """On the branch ZeroToHeroPuzzles of ZeroToHero, read all the levels in src/levels
   and fix every puzzle with this mistake: <kind>. Check the fixed puzzles with the
   Frontend parser and type system, through a short program in your scratchpad that
   is never committed.
   Improve anything else you see that is clearly improvable. Never rewrite text
   marked Good in ZeroToHero/tasks/ruledOut/ZeroToHeroPuzzles into its Bad
   form; where no Bad form is given, leave the Good text as it is.
   Regenerate the html of the game with the level editors in src/htmlMangle and open
   the changed levels in a browser. Commit and push. Reply with the changed levels."""
4. Read the diff of the commit. Revert what turns Good text of the ruled out file into Bad text, commit and push.
Stop when the diff of the ZeroToHero puzzles PR passes 1500 changed lines, or the refusal
limit is reached, or the time limit is reached.
Make sure at the end there is a PR for the puzzles, and one for ZeroToHero with the
ideas file and the done tasks.

Report: the content of ZeroToHero/tasks/ideas/ZeroToHeroPuzzles, and the
clickable links to all the opened PRs.
